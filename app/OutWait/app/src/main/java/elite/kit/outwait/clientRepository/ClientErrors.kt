package elite.kit.outwait.clientRepository

import elite.kit.outwait.R

enum class ClientErrors(val message: Int) {
    //EXAMPLE_ERROR(R.string.EXAMPLE_ERROR)
    INVALID_SLOT_CODE(R.string.INVALID_SLOT_CODE)
}
