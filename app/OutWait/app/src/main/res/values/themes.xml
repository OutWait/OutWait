<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OutWait" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/outwait_color</item>
        <item name="colorPrimaryVariant">@color/outwait_color_dark</item>
        <item name="colorOnPrimary">@color/outwait_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
      <item name="android:navigationBarColor">@color/outwait_color</item>
    </style>
</resources>
