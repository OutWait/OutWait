package edu.kit.outwait.server.slot

/**
 * Data class that identification code of a slot.
 *
 * @property code the unique string representation of the code.
 * @constructor Creates the read-only object.
 */
data class SlotCode(val code: String)
